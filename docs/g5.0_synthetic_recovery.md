# G5.0 — 单参数 synthetic recovery（$D_s$）

> **结论：PASS。** 平台现在能从一个**已知真值**生成的电压响应里把该参数找回来。
>
> 这一步证明的是**管道连通**，不是物理结论：
> `PyBOP → parameter override → PyBaMM → cost → optimizer → recovered parameter`
>
> 运行命令：
> `python scripts/identification/g5_0_synthetic_recovery.py`
> 报告 JSON：`outputs/fitting/g5.0/g5_0_report.json`（gitignored）

---

## 1. 冻结范围

只回答一个问题：**给定一个在已知 $D_s$ 处生成的 synthetic 电压响应，
当前 identification 管道能不能把它找回来？**

- **一个参数**：$D_s$（`Positive particle diffusivity [m2.s-1]`）
- **一份 synthetic 观测**，三个初值共用
- **不碰**真实 Chen2020 拟合、不碰第二参数、不碰 Agent
- 优化变量用 $z=\log_{10}D_s$，**不是** $D_s$（$10^{-15}$ 量级在线性尺度上优化是自找麻烦）

---

## 2. 配置

```text
dataset / cell / rate    chen2020 / 02 / C2
model                    SPM
parameter_set            Chen2020（A-exact：该参数集即为此电芯标定）
D_s_true                 2.0e-15 m2/s      z_truth = -14.69897
z_bounds                 [-16.0, -13.0]
z_inits                  log10(1e-15), log10(4e-15), log10(1e-14)
optimiser                pybop.SciPyMinimize(method="Nelder-Mead", maxiter=400)
observation              284 点，t_end = 7090.5 s，V ∈ [2.8730, 4.1243] V
```

---

## 3. 四项验收（全部 PASS）

### ① 三个初值进入同一 basin — **PASS**

| 初值 $D_s$ | 恢复 $D_s$ | 相对误差 | 评估次数 |
|---|---|---|---|
| $1\times10^{-15}$ | `1.999979e-15` | **0.001 %** | 31 |
| $4\times10^{-15}$ | `1.999815e-15` | **0.009 %** | 29 |
| $1\times10^{-14}$ | `1.999979e-15` | **0.001 %** | 29 |

basin 内散布 **0.00004 dex**（容差 0.05）。三个初值跨越一个数量级，全部收敛。

### ② 接近真值 — **PASS**

最差相对误差 **0.009 %**（容差 5 %）。

### ③ 显式 cost 曲线在真值附近有明确最低点 — **PASS**

不只信优化器，扫了 $J(\log_{10}D_s)$：

**粗扫（13 点，0.2 dex）**

| $z$ | $D_s$ | $J$ |
|---|---|---|
| −15.7 … −15.1 | ≤ 7.9e-16 | **unreachable**（覆盖 < 50%，见第 5 节） |
| −14.900 | 1.259e-15 | 4.241528e-01 |
| **−14.700** | **1.995e-15** | **7.731658e-06** |
| −14.500 | 3.162e-15 | 1.984272e-01 |
| −14.300 | 5.012e-15 | 5.504070e-01 |
| −13.300 | 5.012e-14 | 1.464881e+00 |

**细扫（15 点，分辨率 0.043 dex，围绕粗扫 argmin）** —— 干净的 V 形：

```text
z=-14.7857  J=6.42e-02
z=-14.7429  J=1.52e-02
z=-14.7000  J=7.73e-06   <-- argmin
z=-14.6571  J=1.18e-02
z=-14.6143  J=4.45e-02
```

**argmin 距真值 0.0010 dex**（容差 0.15）。
**真值处实测 $J = 0.000\times10^{0}$** —— 精确为零，因为那份观测就是平台在真值处的输出。

### ④ 每次候选评估可追溯 — **PASS**

**92 次评估，0 次缺 provenance。** 每条记录都有：

```text
requested   {"Positive particle diffusivity [m2.s-1]": <asked>}
applied     key / old（从参数集读）/ new / source / rate_slug
sources     调用方声明的来源表
json_path   该次评估的 run_metadata.json（平台自己写的）
overlap_fraction / n_points / status
```

审计文件：`outputs/fitting/g5.0/audit/*/…_evaluations.json`，
逐条：`…/audit/*/eval_0001.json`。

---

## 4. 这一关发现并修掉的两个**判据错误**（值得记）

两次失败都不是物理问题，而是**我把验收标准写错了**。记下来是因为它们很容易重犯。

### 错误 A：容差比网格间距还小

初版 `argmin 距真值 ≤ 0.15 dex`，但粗扫 7 点的**间距是 0.4 dex**。
**一个网格对最低点的定位精度不可能优于它自己的步长** —— 这条判据实际测的是网格分辨率，不是物理。
真值恰好落在两个网格点之间，于是"失败"。

**修法**：容差取 `max(名义容差, 实测分辨率)`；并加一层**细化扫描**（围绕粗扫 argmin），
让最终结论报在实际达到的分辨率上（0.043 dex）。

### 错误 B：用线性插值去估"真值处的 cost"

为防粗网格骗人，我加了第二条：**真值必须不差于网格最低点**。
但我用 `np.interp` 插值，得到 2.9e-04，把真值判成了"更差"。

原因是**这个最低点极窄**（约 $10^{-3}$ dex 宽），线性插值在这里暴涨 4 个数量级 ——
那是**插值器的性质，不是成本曲面的性质**，不该让判据失败。

**修法**：**直接把真值加进去实测**，不插值。实测值 0.000e+00。

> 教训：*在一个尖峰型最低点附近，插值不能替代测量。*
> 同理，"优化器停在这里"不能替代"这里是最小值" —— 所以扫描不能省。

---

## 5. 一处必须说明的设计选择：成本定义在**重叠时间窗**上

$D_s$ 越大，到达截止电压越晚；越小则越早。所以**候选的仿真窗长度依赖于候选本身**，
而观测网格是固定的。

处理方式：每次评估只在 $t \le \min(t_{\rm obs,end}, t_{\rm sim,end})$ 上打分，
并在审计里记录**覆盖比例**。覆盖 < 50 % 的候选直接判失败（$J=\infty$）。

这就是粗扫里 **4 个点 unreachable** 的来源：$D_s \le 7.9\times10^{-16}$ 时仿真提前结束，
覆盖不足。**宁可判失败，也不在一个残片上调参** ——
只拿一小段比较会让一个错得很远的参数看起来很好。

---

## 6. 边界（这一关**没有**证明什么）

- **没有**证明 $D_s$ 在真实 Chen2020 数据上可辨识 —— 那是 G5.2。
  synthetic 恢复成功只说明管道通，不说明这个参数相对于真实数据是**可区分**的。
- **没有**证明 $D_s$ 与其它参数不互相补偿 —— 那是 G5.1（二维）。
- 单参数、单倍率（C2）、单模型（SPM）。换倍率/模型不保证结论同样成立。
- 观测是平台自己的输出，**不是实验数据**。这里的 zero residual 是构造出来的，不是拟合出来的。

## 7. 下一步

1. **G5.1**：$\theta=\{D_s,\epsilon_{am}\}$ 二维 synthetic recovery → 画二维 cost landscape，
   看是否出现参数补偿（狭长谷）。
2. **倍率选择**：先用敏感度扫描定"哪几个倍率真正激发 $D_s$"，再定 fit / held-out 划分。
   $D_s$ 在低倍率下灵敏度可能太弱。
3. **G5.2**：真实 Chen2020 拟合。
4. **G6**：未参与辨识的倍率上的预测增益。
